class sharks extends Monster 
{
    constructor({name, imageSrc, framerate, scale = 1.2, collisionPos, eCollisionBlock})
    {
        super({name, imageSrc, framerate, scale, collisionPos, eCollisionBlock})
    }
}